ignore-scripts=true
audit=true

# Le starter Breeze déclare des peer dependencies Vite contradictoires
# (laravel-vite-plugin exige vite 8, @vitejs/plugin-react plafonne à vite 7).
# On installe sans imposer les peer deps plutôt que de forcer un arbre cassé.
legacy-peer-deps=true
